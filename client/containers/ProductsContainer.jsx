import React from 'react';

const ProductsContainer = () => {
  const productsList = ['apple'];
  const [products, setProducts] = useState(productsList);

  return <div></div>;
};

export default ProductsContainer;
